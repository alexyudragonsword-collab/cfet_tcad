"""PySide6 desktop UI, interaction model patterned on Sentaurus Workbench.

Optional dependency: install with ``pip install -e '.[gui]'`` and launch
via ``cfet-tcad-gui``.  Nothing in the core package imports Qt.
"""
