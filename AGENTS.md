# STACKED CMOS TCAD 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只包含一行 `@AGENTS.md`，使 Claude Code 读到同一套规则；Codex 直接读本文件。

## 项目一句话

开源 CFET / 堆叠纳米片器件 TCAD 仿真系统，基于 Python + DEVSIM + gmsh + PySide6，对标 Synopsys Sentaurus 工具链。

> 本文件由 `cairn init` 生成，已填入本项目自身的定位与 provider 配置；其他项目应各自运行 init 后再复用。

## Init 配置

- 毕业 provider：暂无（暂缓对接 —— 首次毕业时再接入知识库）
- 知识库索引：尚未配置
- 毕业目标：尚未配置

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 读 `cairn/ROADMAP.md`，了解路线图、当前重点与开放问题。
3. 读 `cairn/LOG.md` 最新几条（最新在最上），了解近期进展与关键决策。
4. 按当前任务需要，读相关的 `cairn/` 知识专题文档。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 很少变动 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 存根 | 写一次，之后不再改动 |
| `cairn/ROADMAP.md` | 路线图与进展 | 原地更新，保持精简 |
| `cairn/LOG.md` | 时序日志 | 新条目加在最上（最新在前），每条 ≤ 20 行，只写摘要 + 指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 原地更新；坑点写进正文小节并用 `contains` 标注；修订留 LOG 指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；只追加 |
| `cairn/Cited.md` | 知识库引用清单 | 只放指针，绝不复制原文 |

> 其余文件只在出现具体信号时才创建（需要记录一个决策、解决了一个坑、出现跨会话的目标），不预建空壳。工程资产（被代码或流程消费的契约/配置/规格）不由本系统管理，留在代码树里，不进 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则级冲突由本文件裁定。
- 业务/设计结论以 `cairn/` 知识专题文档中的最新记录为准，而非更早的 LOG 条目。

## 知识库消费反射

- 在开展"其可复用内核（它产出或依赖的任何结论）够格毕业"的工作前，先查阅本项目自己的 `cairn/` 知识专题文档；目前尚未接入外部知识库（provider 暂缓对接 —— 见上方 Init 配置），因此外部索引检查与 `cairn/Cited.md` 引用在接入后才启用。

## 文档协作规则

- 动手改之前，先判断用户要的是"讨论/建议"还是"直接改文档"；当用户说"先看看 / 先评估"时，先给分析，不要直接重写正式文档。
- 修正过去的判断时，追加更正说明，不要静默覆盖。
- 不要把未经确认的判断写成既定事实。

## 知识沉淀规则

- 每次实质性推进后，在 `cairn/LOG.md` 顶部加一条（摘要 + 指针）；让结论沉淀进 `cairn/` 知识专题文档。
- **完成回复闸门**：在做出任何完成性声明之前——包括但不限于工作已完成/已实现、已定稿、已更新、已同步、已验证或测试通过；问题已修复或已解决；交付物可用；工作已结束的表述；以及语义等价的说法——先执行 `references/maintenance.md` 里的 Cairn 检查点，只更新其触发矩阵要求的记录并核验，然后再回复。明确的只读 / 不改动要求禁止任何 Cairn 写入。
- 跨项目可复用的经验，在接入知识库后经毕业机制沉淀（provider 暂缓对接 —— 见上方 Init 配置）。

---

# 本项目既有的长期约定

> 以下规则原本记录在 `CLAUDE.md`，Cairn 初始化时迁入本文件（`CLAUDE.md` 改为一行存根）。内容为持久约定，不随会话失效。

## Plan-mode 计划归档

凡是走了 `/plan` 模式并经 `ExitPlanMode` 批准的计划，**执行完后必须归档**（不必每次再被要求）：

1. 计划全文写入 `docs/dev_plan_<slug>.md`，末尾补一段"结果"，注明结果与落地提交。
2. 更新 `docs/PROJECT_DEV_PLAN.md`（全项目唯一的滚动计划/状态文档）：补/改对应阶段条目；在"Plan-mode 计划归档索引"表加一行；统计数字（行数/测试数/配置数/提交数）若已漂移则刷新。
3. 计划全文追加到 `docs/DEV_PLANS_ARCHIVE.md`（按时间顺序的全文归档）。
4. 归档文件与功能改动一并提交；若归档文件在实现完成后才存在，则作为紧随其后的提交。

仅适用于真正走过 plan-mode 的工作；快速修复、直接问答、临时调试不需要归档条目（但若分量较重，仍值得在 `PROJECT_DEV_PLAN.md` 的阶段历史里留一行）。

## 其他长期约定

- Windows 打包工作流（`windows-exe.yml`、`windows-nuitka.yml`）**只允许手动触发**（`workflow_dispatch` 或 `v*` 标签）——未经用户明确要求，绝不加回分支 push 触发条件。推送了与 Windows 构建相关的改动后，提醒用户去 Actions 页手动触发。
- 提交信息以 `Co-Authored-By` 与 `Claude-Session` 结尾（确切格式见近期 `git log`）；提交内容中任何位置都不得出现具体的模型标识/名称。
- 在会话任务描述指定的分支上开发（当前为 `claude/cfet-tcad-simulation-zh2kfo`）；`main` 仅在用户明确批准时才动。
